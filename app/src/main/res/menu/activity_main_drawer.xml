<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:checked="true"
            android:icon="@drawable/ic_inicio"
            android:title="@string/home_item" />
        <item
            android:id="@+id/nav_info"
            android:icon="@drawable/ic_infobar"
            android:title="Acerca de" />
        <item
            android:id="@+id/nav_partes"
            android:icon="@drawable/ic_incidencias"
            android:title="Ver partes" />
    </group>

    <item android:title="Configuración">
        <menu>
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:title="Cerrar Sesión" />
        </menu>
    </item>

</menu>
